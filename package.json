{
  "name": "rhymix-board-skin-lucent",
  "version": "1.0.0",
  "description": "Lucent — a glassmorphism board skin for Rhymix, built with Tailwind CSS v4. Pairs with the Lucent layout.",
  "license": "GPL-2.0-or-later",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./board.lucent.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./board.lucent.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
